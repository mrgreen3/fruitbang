<?xml version="1.0"?>
<labwc_config>

  <!-- Placement -->
  <placement>
    <policy>cursor</policy>
  </placement>

  <!-- Theme -->
  <theme>
    <name>AB_Summer</name>
    <cornerRadius>6</cornerRadius>
    <font place="ActiveWindow">
      <name>Sans</name>
      <size>10</size>
      <slant>Normal</slant>
      <weight>Normal</weight>
    </font>
  </theme>

  <!-- Margins  add optional output="<MONITOR>" -->
  <margin top="5" bottom="3" left="5" right="5" />

  <!-- Keyboard -->
  <keyboard>
    <numlock>on</numlock>
    <repeatRate>25</repeatRate>
    <repeatDelay>300</repeatDelay>

    <!-- Window Management -->

    <!-- Centre 80% -->
    <keybind key="W-C-c">
      <action name="SnapToRegion" region="center-80"/>
    </keybind>

    <keybind key="W-C-m">
      <action name="ToggleMaximize"/>
    </keybind>

    <!-- Snap binds double hit arrow keys to snap window to edge -->
    <keybind key="W-C-Left">
      <action name="SnapToEdge" direction="left" combine="yes"/>
    </keybind>
    <keybind key="W-C-Right">
      <action name="SnapToEdge" direction="right" combine="yes"/>
    </keybind>
    <keybind key="W-C-Up">
      <action name="SnapToEdge" direction="up" combine="yes"/>
    </keybind>
    <keybind key="W-C-Down">
      <action name="SnapToEdge" direction="down" combine="yes"/>
    </keybind>

    <keybind key="W-C-d">
      <action name="SetDecorations" decorations="full"/>
    </keybind>
    <keybind key="W-C-b">
      <action name="SetDecorations" decorations="border"/>
    </keybind>

    <!-- Workspaces -->
    <keybind key="W-1">
      <action name="GoToDesktop" to="1"/>
    </keybind>
    <keybind key="W-2">
      <action name="GoToDesktop" to="2"/>
    </keybind>
    <keybind key="W-3">
      <action name="GoToDesktop" to="3"/>
    </keybind>
    <keybind key="W-4">
      <action name="GoToDesktop" to="4"/>
    </keybind>

    <!-- Send to workspace -->
    <keybind key="W-S-1">
      <action name="SendToDesktop" to="1"/>
    </keybind>
    <keybind key="W-S-2">
      <action name="SendToDesktop" to="2"/>
    </keybind>
    <keybind key="W-S-3">
      <action name="SendToDesktop" to="3"/>
    </keybind>
    <keybind key="W-S-4">
      <action name="SendToDesktop" to="4"/>
    </keybind>

    <!-- Window actions -->
    <keybind key="W-q">
      <action name="Close"/>
    </keybind>

    <!-- Window switching (Alt-Tab) -->
    <keybind key="A-Tab">
      <action name="NextWindow"/>
    </keybind>
    <keybind key="A-S-Tab">
      <action name="PreviousWindow"/>
    </keybind>

    <!-- Applications Menu (Super+d) -->
    <keybind key="W-d">
        <action name="Execute" command="/home/ablive/AB_Scripts/wmenu-launcher"/>
    </keybind>

    <!-- Applications -->
    <keybind key="W-t">
      <action name="Execute" command="foot"/>
    </keybind>
    <keybind key="W-Return">
      <action name="Execute" command="foot"/>
    </keybind>
    <keybind key="W-w">
      <action name="Execute" command="firefox"/>
    </keybind>
    <keybind key="W-f">
      <action name="Execute" command="thunar"/>
    </keybind>
    <keybind key="W-e">
      <action name="Execute" command="l3afpad"/>
    </keybind>
    <keybind key="W-g">
      <action name="Execute" command="~/AB_Scripts/gparted-launcher"/>
    </keybind>
    <keybind key="W-m">
      <action name="Execute" command="foot btop"/>
    </keybind>

    <!-- Right-click menu -->
    <keybind key="W-space">
      <action name="ShowMenu" menu="root-menu"/>
    </keybind>
    <keybind key="W-S-space">
      <action name="ShowMenu" menu="client-menu"/>
    </keybind>

    <!-- Reconfigure -->
    <keybind key="W-S-r">
      <action name="Reconfigure"/>
    </keybind>

    <!-- Screenshot -->
    <keybind key="Print">
      <action name="Execute" command="screenshot"/>
    </keybind>
    <keybind key="W-S-s">
      <action name="Execute" command="screenshot"/>
    </keybind>

    <!-- Display configuration -->
    <keybind key="W-C-F12">
      <action name="Execute" command="wlr-randr --output Virtual-1 --mode 1920x1080"/>
    </keybind>

    <!-- Volume Control (PipeWire) -->
    <keybind key="XF86AudioRaiseVolume">
      <action name="Execute" command="wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+"/>
    </keybind>
    <keybind key="XF86AudioLowerVolume">
      <action name="Execute" command="wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-"/>
    </keybind>
    <keybind key="XF86AudioMute">
      <action name="Execute" command="wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"/>
    </keybind>

    <!-- Brightness Control -->
    <keybind key="W-equal">
      <action name="Execute" command="brightnessctl set +5%"/>
    </keybind>
    <keybind key="W-minus">
      <action name="Execute" command="brightnessctl set 5%-"/>
    </keybind>

    <!-- Lock Screen -->
    <keybind key="W-l">
      <action name="Execute" command="swaylock -c 1e1e2e"/>
    </keybind>

    <!-- Exit -->
    <keybind key="W-A-e">
      <action name="Exit"/>
    </keybind>
  </keyboard>

  <!-- Mouse -->
  <mouse>
    <default/>
    <context name="Frame">
      <mousebind button="A-Left" action="Drag">
        <action name="Move"/>
      </mousebind>
      <mousebind button="A-Right" action="Drag">
        <action name="Resize"/>
      </mousebind>
    </context>
    <context name="Top">
      <mousebind button="Left" action="Drag">
        <action name="Move"/>
      </mousebind>
      <mousebind button="Left" action="DoubleClick">
        <action name="ToggleMaximize"/>
      </mousebind>
    </context>
    <context name="TLCorner">
      <mousebind button="Left" action="Drag">
        <action name="Resize"/>
      </mousebind>
    </context>
    <context name="TRCorner">
      <mousebind button="Left" action="Drag">
        <action name="Resize"/>
      </mousebind>
    </context>
    <context name="Root">
      <mousebind button="Right" action="Press">
        <action name="ShowMenu" menu="root-menu"/>
      </mousebind>
    </context>
  </mouse>

  <!-- Desktops -->
  <desktops number="4">
    <prefix></prefix>
  </desktops>

  <!-- Snap Regions -->
  <regions>
    <region name="center-80" x="10%" y="10%" width="80%" height="80%" />
  </regions>

  <!-- Window rules -->
  <windowRules>
  </windowRules>

  <!-- Resize edges -->
  <resize>
    <popupShow>Never</popupShow>
  </resize>

  <!-- Focus -->
  <focus>
    <followMouse>yes</followMouse>
    <followMouseRequiresMovement>yes</followMouseRequiresMovement>
    <raiseOnFocus>no</raiseOnFocus>
  </focus>

  </labwc_config>
